{
  "name": "foresight-forge-build",
  "private": true,
  "description": "Vercel build wrapper to run the Python forecasting pipeline and emit docs/",
  "scripts": {
    "vercel-build": "uv venv .venv --python python3 && uv pip install -r requirements.txt -p .venv/bin/python && .venv/bin/python forecast.py run-daily"
  }
}
